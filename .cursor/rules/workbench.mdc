---
description: 工作台作为生产面的完成度，防止把接线当成完成或把图合同修回 V1/V2
globs: web/src/pages/workbench/**/*,go/internal/graph/**,web/src/lib/i18n.ts,docs/USER_GUIDE.md
alwaysApply: false
---

# 工作台

改画布、检查器、图命令、配方或侧栏时读本规则。操作说明以 `docs/USER_GUIDE.md` 为准。1440 / 1024 / 390 连续动作已由 `just web-e2e-live-graph` 证明。

```tsx
// ❌ 新卡片、新壳、V2 mutation、plan key、浏览器 undo 权威
// ❌ 把 Redo 映射成再调一次 undo
// ❌ 加深 GraphNodeInspector 私有字段（目的地是 Catalog config_fields）
// ❌ 空选展示版本号；运行第一屏铺 digest / id
// ❌ 失败只写在卡片或运行页
// ❌ 用 GraphProposal 顶替用户直接编辑；为 V1 archive 开平行编辑器

// ✅ 复用 workbench/chrome 槽位并喂数据
// ✅ 持久操作走 ChangeSet；选择/视口/预览留在客户端
// ✅ 打开详情能看见失败并重试；空选是下一步
// ✅ 忙时锁定添加、详情、绑定、配方
```

禁止恢复：不可删除血缘边、多输入 handle 排、顶部流程统计条、用选区/坐标推断边。分组仍是一层视觉组织，没有端口、运行、取消、重试或嵌套。绑定 ≠ 连边 ≠ 子图库。390px 浏览 / 编辑 / 选择三模式有图标；主操作不是 hover-only。
