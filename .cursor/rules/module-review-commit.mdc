---
description: 自己负责的模块做完先 review，确认无误立刻 commit，禁止把已验收 diff 堆在工作区
alwaysApply: true
---

# 模块完成：Review 后立即 Commit

负责某个切片的人，在该切片可验收后先审自己的 diff，通过就立刻提交。不要等用户再说「请提交」，也不要把已审过的改动留到下一刀。

## 完成闸门

切片完成 = 任务包里的用户可见行为已落地，约定测试/证据已跑过，且没有改别人的文件。UI 改动还要按仓库要求做完浏览器（或替代）验证。

## Review（提交前）

只看本切片 `git diff`（含未跟踪文件），对任务包逐项核对：

- 行为与契约一致；没有顺手改无关文件
- 无调试残留、密钥、生成物、`.env`
- 测试/边界仍站在约定层；主代理还要看并发改动有没有撞车

有问题先修再审。带着未决风险、失败测试或说不清的 diff 不提交。

## Commit（审过立刻做）

只 `git add` 本切片文件。提交前用 `git diff --cached --name-only` 核对暂存区 = 本切片归属。message 用 Conventional Commit + 中文写 why，一次一个主题。不 push、不 `--amend` 别人的提交、不跳过 hook。

工作区里别人的脏文件、用户已有改动、下一刀还没做的东西一律留下。

```text
❌ 切片做完就去开下一刀，工作区堆着未提交 diff
❌ 把本切片和无关的 go/web/docs 改动塞进同一次 commit
✅ 自审 → 只暂存本切片 → commit → 再开始同一负责人的下一刀
```

## 谁提交

并行子代理只自审并报告「可提交」：changed files、测试、风险、假设。不 commit、push、reset、revert，也不宣布总任务完成。主代理复审 diff、跑共享边界检查，通过后在当轮提交该切片。同一时刻只有一个 git 写者；并行切片谁先做完谁先交，不要攒齐再一次性提交。

主代理自己改的切片：自审通过后自己立刻提交。同一负责人的下一刀，要等上一刀已经进 git。
