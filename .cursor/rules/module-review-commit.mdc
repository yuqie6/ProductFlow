---
description: 准备提交或审核交付时加载；规定自审、选择性暂存和任务级提交
alwaysApply: false
---

# Review 与 Commit

负责切片的人在验证后审自己的完整 diff（含未跟踪文件）：行为符合合同、测试覆盖修改边界、无越界文件、密钥或调试残留。主代理审核子代理交付及并发影响；主代理自己执行时明确记录自审。

本任务首次准备提交时读取本文件；看板任务同时加载任务协议。规则未变化且上下文完整时复用，变化后刷新。看板任务默认一项完成任务一个交付 commit。直接交办的完整设计、功能、迁移或修复按可独立审阅且已验证的主题提交，可以包含多个提交；中间提交不代表完整目标已验收，也不触发建单。已审核交付由指定 Git 写者及时提交，不攒成多个任务的大提交。共享工作区同时只有一个 Git 写者，默认主代理。

每次提交前检查当前 Git 状态与归属，按已审核 diff 暂存；共享文件只暂存本任务变更。核对 `git diff --cached` 和 `git diff --cached --name-only`，确认没有用户或其他任务的改动、密钥、临时产物或未解决问题。message 用 Conventional Commit + 中文写 why。不 push、不 amend 他人提交、不跳过 hook。提交失败时保留现场，解决当前失败后继续；检查提交结果再宣告交付。

内部 issue 的记录、候选提交例外和归档由 [`docs/audits/tasks/README.md`](../../docs/audits/tasks/README.md) 定义。认领、进度、阻塞和释放占用不单独提交。普通直接修复验证和自审通过后提交，不强制建单。清理只处理本任务创建且已无用途的临时文件或进程；保留共享证据及他人的未提交改动，不 reset、不 revert。子代理交付后等分配，主代理按 `AGENTS.md` 的完成标准继续原目标内的必要工作。
