---
description: 验收任务板：一人一任务，先认领再改代码，做完归档后停下
alwaysApply: true
---

# 验收任务板

从 `docs/audits/tasks/` 接活时按看板走，一人同一时刻只认领一份：

1. 只挑看板里 `开放` 的任务；独占文件与已 `认领` 的任务相交则不准接。
2. 在任务文件和 `docs/audits/tasks/README.md` 写下认领者，**先提交认领**，再改代码。
3. 只读那一份指导。不要从总账本开工。
4. 做完自审 → 提交功能 → `git mv` 到 `tasks/archive/` → 按「完成后可拆」新建下一份 `开放` 任务（不要认领它）→ 提交归档 → **停下等分配**。

协议全文：[`docs/audits/tasks/README.md`](docs/audits/tasks/README.md)。用户随口修的 bug 不走看板。
