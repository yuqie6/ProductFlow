# 任务：核心商品图的真实质量对照与差距诊断

状态：阻塞
类型：证据
认领者：—
认领于：—
业务组：图片质量
父账本：image-quality.md
完成后可拆：依据有效视觉差距发布生成链修复，不自动重复刷分

## 问题来源

用户要求实际测评生图质量，并比较项目图片与优秀商品图的差距，强调这是项目核心竞争力。原池首轮启动后视觉复核发现：QCY 入耳耳机卖点槽是头戴耳机广告，珀莱雅参考为会员礼品合集，BENNS 参考只有海盐原料；多个图种按位置错标。原 42 槽位中止且保留，不作为质量基线。

## 做成什么样

同一批 8 SKU，主图、卖点、场景、细节各 1 张，工作台/直调/商家原图三方对照。保留四维评分和图片，逐图判断身份、信息、构图及可用性。说明金标也可能有商业噪声，不将来源平台等同于优秀设计。诊断结果不替代原 42 槽位门槛，不声称全池或生产泛化验收。

## 前置与并行

- 固定候选 36ade164，checkout `/tmp/productflow-image-quality-0905`，不修改候选、harness、judge、naive 或门槛。
- 基于已查看的 8 张整套 contact sheet 校正身份参考和 4 类金标；逐项来源路径记录于 `storage-dev/image-quality-benchmark-0905/selection.json`。在查看工作台输出和评分前选定，之后冻结。
- 独占 DB `productflow_image_quality_0905_calibrated`、Redis 16529、API 29422、产物根 `storage-dev/image-quality-0905-calibrated`；原中止批次另目录保存，独立进程均已结束。
- 当前 prompt=gpt-5.5、image=openai_responses/gpt-5.6-luna，同一 image 绑定对照，评委 gpt-5.5；配置从共享库只读复制到独立库，运行前后比对 hash。
- 用户已授权实际对照。固定 8×4=32 槽位、64 张生成图和 32 次三方评审，加工作台提示词调用；不自动增加后续批次。
- 首批完成 3c/appliance 后，共享 PostgreSQL 于 23:53:59 收到 fast shutdown，导致其余 6 个 case HTTP 500。保留原报告；剩余 6 个 case 使用独立容器 `productflow-image-quality-pg-0905`（15439）、DB `productflow_image_quality_0905_remaining`、Redis 16539、API 29432、产物根 `storage-dev/image-quality-0905-remaining` 续测。只复制原独立库的冻结配置和剩余 manifest，不重跑已完成的 8 个图位。

## 只改这些文件

- 本任务、父章程、原任务交接和看板；必要比较报告位于本组证据文档。
- 本地校正 manifest、图片、比较页、执行脚本和日志仅进入隔离产物目录或 /tmp，不改原 pool。

## 合同与验证

- 校正集每个 SKU 有身份参考，参考/金标 SHA-256 不重叠；保留真实原图字节和来源，不能合成金标。
- 全部 32 槽位成功生成并取得四维评分才形成完整对照；失败和缺失必须单列，不能用部分分数假报完成。
- 保留 run_id、代码/配置/manifest hash、逐图对照与主要缺陷；区别模型分数、主代理视觉观察及因果推测。
- 全过程 checkout 与配置固定，结束后检查进程及产物，just docs-check 和完整 diff 自审；按任务提交，不 push。

## 阻塞与交接

- 原因：32 图位合同尚缺 8 个有效三方评分。浴巾的创作要求/系列风格调用为 unknown；跑鞋出图 3 张后工作流 unknown，均未取得直调和评分。需要查明供应商结果并明确后续重跑范围，不能自动重试未知副作用或将缺失计为质量零分。
- 跟进者：主代理-image-quality-0905-2245。
- 解除条件：确认浴巾与跑鞋未知调用的供应商结果，固定后续采证范围与预算；补齐缺失三方评分后按原 32 图位合同验收。
- 交接：本轮独立 API/worker/dispatcher/runner/Redis 均已结束，独立 PostgreSQL 容器已停止并保留数据。原始报告、未评分的跑鞋输出和校正 manifest 保留，后续不得覆盖首轮结果。

## 证据

- 原批次：首 SKU QCY 的部分工作台图片已生成，发现测量材料错误后停止；无完整 report，不参与本批评分。
- 校正集：8 SKU、32 槽位，保持原抽中的 8 个商品。仅纠正参考身份和图种，未修改被测代码；原始池保持不变。
- 首批 `20260905T152704Z-546201ca`：8 个图位有完整三方评分，剩余 6 个 case 因数据库中断无评分。不得使用该报告的 0/8 case 通过率代表图片质量；有效图位结果与环境失败分开汇总。续测只覆盖缺失 24 图位。
- 续测 `20260905T155629Z-62325dfc` 已结束。合计 6 个商品、24 个图位有三方评分：工作台/直调/商家原图均分 4.4583/4.3438/3.8646；对直调 11 胜6平7负；10/24 图位过线。任务保留阻塞，不能以部分结果关闭 32 图位合同。
- 原图对照页：`storage-dev/image-quality-0905-calibrated/evidence/comparison.html`；完整分数 `summary.json`；逐图观察 `visual-observations.json`。27 张工作台输出中 24 张送评文件与展示图片 SHA-256 一致，另 3 张跑鞋输出未评分。页面 1440/390 宽度无横向溢出，99 张已引用图片无缺失。
- 冻结检查：候选 36ade164；前后配置 hash 均为 `a917986397ba58a88d84509c646b581fe1d65488563b9ed7f6b43328fe20bfbc`；原 8 个及续测 6 个 manifest hash 未变，checkout 干净。首批数据库中断后的配置复核记录为 `provenance-check.json`，不能伪造当时未写成的 after.json。
- 用户指出创建页已有 AI 商品说明/规格填写。已核实页面调用 `/api/v2/product-source-notes/generate` 后将表单作为 source_note 提交；本轮 harness 仅用标题和空 props 构造 source_note，没有调用该功能。因此本轮不能代表完成 AI 填表后的完整商家流程，也不能把属性为空归为产品缺少提取能力。
- 后续系列作用域设计仅讨论，未修改生成代码、评委或冻结金标。
