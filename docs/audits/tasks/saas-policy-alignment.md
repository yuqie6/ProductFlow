# 任务：按用户裁定纠正未知调用额度与图片采用政策

状态：阻塞
类型：实现
认领者：—
认领于：—
完成后可拆：用户裁定后按额度与采用两个独立结果安排

任务状态、认领和提交遵循 [Issue 协议](README.md)。

## 问题来源

[设计校准](archive/saas-design-alignment.md) 已纠正文档和明显占位代码，但以下两个产品政策尚未得到用户答复。把它们留为明确待决工作，避免既有实现被当成用户认可，也避免阻塞无关账号与管理员商品功能。

## 做成什么样

用户明确供应商结果无法确认时的用户额度处理，以及有限图片检查失败时用户是否仍可确认采用。按裁定修改真实业务边界和受影响界面，保留原始执行事实与历史评测结果。

## 待裁定

1. `go/internal/quota/unknown.go` 当前默认 72h 到期按预留全额结算。建议到期释放用户预留、保留平台未知成本事实；替代方案是等待管理员核对，不自动全额扣除。尚未授权任何一个新方案。
2. `go/internal/delivery/adoption.go` 的 `CreateAdoption` 当前消费文字/路线检查并拦截。建议有限检查失败或无法判断时展示问题，允许用户明确确认采用，质量状态不伪装为通过；替代方案是仅对已证明可靠的检查保留拦截。越权、损坏等确定性失败仍须拒绝。尚未授权任何一个新方案。

## 前置与并行

政策需要用户答复；当前无代码、数据库或运行资源占用。每个答案到达后可独立推进相应结果，无需等另一个。认领前按实际调用链登记 quota/执行回收或 delivery/图片检查及 Web reader 的排他范围，不修改模型评分、冻结样本与真实 provider 配置。

## 合同与验收

- 额度变化不把 unknown 伪造成供应商成功或零成本。扫描、人工处理和迟到结果竞争只能形成一次有效余额变化；覆盖重试、幂等和原始关联记录。
- 采用权限、用户确认和质量状态分别表达；确认采用不能修改历史检查结果或宣称图片合格。覆盖受影响 API、持久快照、下载与 Web 状态。
- 决策回写 ROADMAP、商家平台、图片质量和体验组的受影响条款；历史证据不重算。
- 运行受影响 Go/PG、Web 回归和静态门、docs-check。真实付费评测不由本任务自动授权。

## 阻塞与交接

- 原因：用户尚未回答两个产品政策问题，运行行为保持现状。
- 解除条件：获得对应政策答复，并将该结果的最终行为与验收条件写明。
- 跟进者：主代理。
- 交接：设计纠偏切片已交付；本任务未开工，无共享资源占用。

## 证据

- 2026-09-08：当前实现入口已定位；本会话已提出建议与替代方案，尚无答复。
- 交付定位：随本任务提交。
- 审核者 / 结论：待审核。
- Issue 结果 / 剩余缺口：未交付；不能用文档校准代替政策实现。
