# 任务：商品工作台的功能衔接与分层操作

状态：完成
类型：实现
认领者：frontend_astra
认领于：2026-09-08T03:55:04+08:00
完成后可拆：无

任务协调遵循 [协议](../README.md)。

## 问题来源

用户要求普通操作可直接得到满意效果，高级控制可选；各功能共用商品、素材、任务和结果，避免孤岛。

## 做成什么样

沿商品资料到生成、修改、采用、下载与复用检查当前断点，并修复一个有真实证据的完整用户路径；不另建工作台或执行器。

## 前置与并行

基线 6d431274。主代理统一协调；两任务不重叠代码写入，活文档与 Git 由主代理持有。保留既有图片质量 smoke 未提交记录。测试使用各自隔离产物与数据库，不修改共享服务配置。用户已授权开发中的必要模型验证，不据此扩大到生产操作。

## 修改范围与所有权

frontend_policy 先只读审 web 工作台、图库、局部编辑、采用与下载调用链，最多报告三个具体断点及优先建议；主代理选定后冻结排他代码范围。不得改首页、图片评委/测试集、Pi、公共 DB/provider 设置。

## 合同与验收

目标路径在真实浏览器完成；保留已有高级控制、候选版本与跨账号隔离。静态检查、针对性回归和与改动相称的 Web 门；只读发现不算实现交付。

## 阻塞与交接

- 原因：无。
- 解除条件：不适用。
- 跟进者：主代理。
- 交接：无运行资源占用。

## 证据

- 交付定位：随本任务提交。
- 审核者 / 结论：主代理审核完整 diff 与窄屏截图；已修复空源节点误报和长列表挤占空间，验收通过。
- Issue 结果 / 剩余缺口：预览、选择与采用版本导出衔接已交付；图库编辑采用目标另属后续设计，真实服务端 ZIP 内容不在本次前端验证范围。

## 冻结实施合同

主代理选定结果预览与采用版本导出的一条衔接路径：

- 结果卡提供可聚焦、可单击/键盘操作的明确预览入口，复用现有图片预览和下载，保留选择行为；预览不产生业务写入。
- 当前结果与已采用槽位不同时，明确提示导出的是已采用版本。比较源节点与资产身份；布局或无关 graph revision 变化不得造成误报。删除节点、缺失当前资产或不同 graph 的旧采用版本也要如实表达。
- 继续允许下载旧采用版本；不得自动覆盖、自动采用或为只读下载新增二次确认。用户明确采用新结果后，相应差异消失。质量问题采用确认沿用现有合同。
- 图库局部编辑缺少采用目标是另一项已知设计缺口，本次保留当前明确的图库行为，不猜测节点归属。

排他代码范围：web 工作台 GraphResultsView、workbenchResults、既有 deliveryAdoption/resultProjection 辅助函数及对应测试，必要调用处、四语言消息、新增专用浏览器测试。共享类型仅在必要的前端投影范围内调整，不改 API 合同。首页、Go、Pi、评测、服务配置和活文档由其他所有者持有。实现者报告实际修改清单供主代理审核。

验收：针对性回归涵盖预览入口和选择隔离、资产变化/节点移除/仅布局变化、旧版本导出及重新采用；浏览器在窄屏与桌面验证操作可达、版本提示与导出目标，记录只读预览无写请求。按 web/AGENTS.md 完成适用检查，不重启共享开发栈。

## 实现者交接

用户指定前端实现使用 gpt-6-astra / low。主代理已中断 frontend_policy 的实现写入；原有未提交修改保留，转交 frontend_astra 审核、完成与验证。frontend_policy 仅提供只读交接，不再持有写入权。

## 最终证据

- 实现者：frontend_astra（gpt-6-astra / low）；frontend_policy 未完成草稿经接手审核后收口。
- 全量 Web：104 文件 / 744 tests、ESLint 通过；审核修正后针对性 23 tests、受影响文件 ESLint、just web-build（含类型检查与包预算）通过。
- 浏览器：原质量确认与连续路径合跑 25 tests；审核最终版连续路径 10 tests，四语言 × 1440 亮色键盘 / 390 暗色触摸、32 图位有界提示、空源节点不误报、预览零写请求、旧 version-1 导出与重新采用后 version-2 导出。路由 mock，不代表真实 DB 或 ZIP 内容验收。
- 最新日志：/tmp/pf-continuity-review-tests.log、/tmp/pf-continuity-review-lint.log、/tmp/pf-continuity-review-build.log、/tmp/pf-continuity-review-browser.log；截图目录 /tmp/pf-continuity-review-browser/。
- 原 adoptedSlotBySlot 入口已删除并扫描所有读者；采用映射与替换按 source_node_id，合法空源节点保留不确定表达。未改变服务端合同与质量确认。
- 主代理完成中英文操作指南、帮助页投影核对、完整 diff 与临时进程审查；未重启共享服务。docs-check 通过后随本任务提交。
