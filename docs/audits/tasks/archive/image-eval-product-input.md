# 任务：冻结图片质量对照的商品 AI 表单输入

状态：完成
类型：实现
认领者：主代理-image-quality-0906-0132
认领于：2026-09-06T01:32:15+08:00
业务组：图片质量
父账本：image-quality.md
完成后可拆：基于相同商品输入验证卖点表达与图组内容分工

## 问题来源

用户将图片质量组整体交由本会话负责。此前真实对照跳过已有创建页 AI 表单，harness 只提交标题与空 props，无法代表商家完成 AI 填表后的输入。原图诊断及限制见 [质量对照](../image-quality-comparison.md)。

## 做成什么样

评测能够通过现有商品说明生成接口准备输入，将结果与参考身份冻结，并在后续对照中复用同一 source_note。已有标题/属性基线仍明确标识。准备失败不得悄悄改用另一输入；金标不进入商品说明或生成请求。交付确定性回归与命令说明，不把此项实现宣称为质量提升。

## 前置与并行

- 前置：已有商品 AI 表单接口；原始诊断图片与评分仅引用，保持冻结。
- 本轮独占 `go/internal/imageeval/`、`go/cmd/productflow-image-evals/` 的必要改动；必要命令入口及本组文档由主代理串行编辑。当前看板没有相交认领；Agent Skill 任务持有其自身目录。
- 不启动共享服务，不修改 provider 或数据库设置。确定性测试使用临时目录与 HTTP 测试服务。
- 本次不新增付费模型批次；后续实测先固定新合同、输入与调用上限。

## 只改这些文件

- `go/internal/imageeval/` 中输入准备、运行入口及相关测试。
- `go/cmd/productflow-image-evals/` 入口、参数回归与必要 `justfile` 命令。
- 本任务、看板、归档索引、图片质量组文档。

## 合同与验证

- 追踪创建页表单提交格式，复用现有 API，不另写卖点提取 prompt。
- 冻结输入绑定商品身份与参考图片内容；不同参考或商品不得误用快照。
- 正式运行开始前验证全批输入，避免运行到中途才发现缺失。
- 记录使用的输入及其身份，使 A/B 可检查相同输入；不改变 judge、naive 或 gate。
- 运行针对性测试、Go 包静态检查、`just docs-check` 与完整 diff 自审，通过后按任务交付提交。

## 阻塞与交接

- 原因：无。
- 跟进者：主代理-image-quality-0906-0132。
- 交接：既有两个阻塞采证任务与其原始证据保持原结论。

## 证据

- 开工基线：`b82cfae5975ba66b8ca901d01ee6777ae1f8a9c4`。
- 已实现 `prepare-inputs --out`：复用商品说明生成 API，每个抽样商品一次调用；保留原始表单、最终 source_note、身份摘要、候选版本和准备时间。失败停止并保留部分输出；输出路径已存在时不调用模型。
- `run --product-inputs` 在登录、创建商品和任何生图调用之前核对整个抽样集；通过后使用冻结文本，报告保存实际输入与文件 SHA。标题/属性基线明确记录，属性排序固定。
- `bash scripts/with_dev_env.sh bash -lc 'go test -C go ./internal/imageeval ./cmd/productflow-image-evals -count=1 -p 1'` 通过。覆盖准备到创建请求/报告的链路、参考输入隔离、缺失/身份不符/空值/重复/不完整/字节变化的整批预检、失败停止和重复准备拒绝、表单空规格与 4000 字符边界。
- `go vet` 对受影响两个包通过；CLI 参数回归验证缺少文件参数和非法抽样参数在连接服务前拒绝。`just docs-check` 与 `git diff --check` 随最终提交复核。
- 审核者：主代理-image-quality-0906-0132，自审。只改评测输入与报告，未改变 judge、naive、gate、生产商品或生成行为；无遗留运行服务。生成表单仍须在实际测评中审核事实正确性。
- Issue 结果：输入冻结实现与确定性链路验证完成。没有真实模型调用或新质量分数；测试 HTTP 服务仅验证输入合同，不替代真实 API/模型质量验收。
- 交付定位：随本任务提交。
