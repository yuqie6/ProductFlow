# 任务：让崩溃恢复测试按实际已提交首批核验

状态：完成
类型：实现
认领者：主代理-eval-quality-restart-0905
认领于：2026-09-05T21:08:00+08:00
业务组：Agent 质量
父账本：agent-eval-system.md
完成后可拆：恢复澄清读取义务任务的完整验证

按 [任务协议](../README.md) 执行。协调者已确认本单所有权，读取义务任务暂交协调者保管，无并发 Agent runtime 写入。

## 问题与范围

`src/process-restart.e2e.test.ts:208` 把 ACK 前崩溃的首批硬编码成 `[1]`。隔离复现首批包含 `turn/start` 与正常上下文注入 `tool/call`，恢复正确确认 `[1,2]` 却被判失败。当前串行、并行及隔离全量均受影响。源代码和锁文件与 `051e3338` 相同。

仅拥有 `agent-service/src/process-restart.e2e.test.ts` 的该测试、本任务及账本/归档索引。禁止改运行时、评测题、Skill、其它恢复场景或共享服务；隔离替身测试无需真实 provider 或 PG。

## 验收

以首次提交且 ACK 未发出的真实事件快照为期望，崩溃恢复后精确核对确认序列、事件内容与顺序不变，首批不重发、provider 未调用、checkpoint 未执行。首批必须非空且始于 turn/start，不接受空快照自证通过。不能硬编码调度决定的批大小。

专项进程恢复四项、完整 Agent 测试、类型检查及 docs-check；自审后单独归档提交，恢复读取义务单。原失败证据保留，不能靠重试或删除断言放行。

## 结果与证据

- 根因补正：ACK 阻塞没有阻止后续 runtime-context 处理，本地可追加上下文 tool/call，恢复请求可能含服务端尚未提交的尾部。只快照服务端首批仍能复现失败。测试为本场景单独阻塞 runtime-context 到 ACK 释放，固定在首批已提交、未 ACK、后续上下文未生成的崩溃窗口；其他三种恢复场景默认不阻塞。
- 崩溃前快照非空且始于 turn/start；恢复精确比较事件内容与确认序列，检查首批没有重发、provider 零调用、checkpoint 为空。未修改运行时、依赖或评测输入。
- 2026-09-05：修复前并行/串行/隔离全量均在同一断言失败；修复后 `pnpm test` 297 passed / 6 skipped（包含同时保管的读取义务回归）；`pnpm exec tsc --noEmit` 通过。临时打印已从隔离副本删除。
- 审核：主代理-eval-quality-restart-0905 自审；本单通过，生产恢复能力未新增或改变。交付定位随本任务提交，以归档 Git 历史查询；不回填本次 hash。
