# 任务：本人偏好与商家资料可保存，站点配置只由管理员管理

状态：完成
类型：实现
认领者：account_frontend
认领于：2026-09-08T08:27:59+08:00
业务组：商家平台
父账本：merchant-platform.md
完成后可拆：无

任务状态、认领与提交遵循 [Issue 协议](../README.md)。

## 问题来源

现有语言/主题是浏览器偏好，商家名称与站点 provider 配置属于不同所有者。一个账号自有一个商家，设置界面应直接表达本人和站点权限，不建设团队设置体系。

## 做成什么样

本人可在账户页面保存个人语言/主题与商家名称。站点 `/settings` 只给管理员；界面语言不改变商品输出语种。沿已有组件和字段实现，不新增无业务读者的设置。

## 前置与并行

[设计校准](saas-design-alignment.md) 已完成；[账户任务](saas-account-team.md) 提供本人页面时复用，若一起交付则由同一主代理整合，不以团队能力为前置。隔离 PG/browser，无需真实 provider。

## 修改范围与所有权

认领后按字段实际读写者冻结 auth、preferences、Account、API/types/i18n 和必要 schema/tests；共享文件单写者。

## 合同

- 只写本人偏好和自有商家资料。locale/theme 沿现有枚举，不新增生产参数或商家自带 provider 密钥。
- 本人设置跨会话可恢复，不将共享浏览器旧值静默上传给新账号；退出后清账号缓存。
- 使用已有模型；并发语义按字段风险明确，语言/主题不强制新增独立 revision/409 协作协议。
- 如增加时区，它只影响显示/筛选，UTC 持久事实与已受理任务不变；没有日期业务读者时不提前引入。
- 站点凭据、模型和运行配置仍属管理员；不恢复旧二次解锁机制。

## 怎么验收

本人/他人读写隔离、刷新与换账号、无效枚举、保存失败恢复；现有四语明暗与桌面/手机可操作。相关 PG/Go 与 Web 回归、静态检查及 docs-check，不跑无关图片实验。

## 阻塞与交接

- 原因：账户与 Ops 页面已交付，前置解除。
- 解除条件：已满足，消费 03482bc9 与 b251f20b。
- 跟进者：开发协调者。
- 交接：本轮开始；共享开发服务不重启，隔离数据库/浏览器。

## 证据

2026-09-08，主代理审核 account_backend / account_frontend 的完整切片，并自审停用商家仍可管理本人安全的最终修正及整合。

- Go：全新隔离 pf_prefs_root_0908 库的完整 auth 通过（最终17.703s），schema 全包通过（23.395s），API 路由合同通过；没有沿用共享旧夹具迁移失败作为结论。新增回归覆盖部分字段不互相覆盖、非法空对象/null/未知字段/枚举、匿名 session 不投影偏好、本人和商家归属、停用商家的偏好/资料/改密/撤销仍可用而商家名写入403。迁移验证默认值、两个 CHECK 和再次迁移保留已保存选择。
- Web：110文件768测试、全量lint/design、TypeScript和生产构建/包预算通过。固定偏好/身份验收28项，四语言×桌面手机×明暗；正常账户操作3项；最终手机离屏失败重试与截图3项。实际同路由切账号后，原账号迟到的注销/改密/撤销不清空新账号会话。原85通过/1失败的广泛矩阵保留，失败为测试未等菜单焦点，修正等待后相关验收通过。证据与最终23文件SHA：`.debug/saas-preferences-settings-20260908/`。
- 真实API与浏览器：`.debug/saas-prefs-20260908/live-3-final/` 验证保存语言/主题与自有商家名、重新加载、独立浏览器登录恢复、退出回匿名设置、另一账号保持默认且无自动偏好上传。真实双账号/无商家Operator、非法请求、暂停商家403和跨会话持久化通过。前两次API/浏览器成功也保留；第三次使用最终Go服务。第三次后仅增加手机错误条sticky样式，业务证据复用，最后三项浏览器验证该差异。
- 最终入口999949字节，gzip274217字节，未提高预算；raw预算只余51字节，不代表后续功能可随意加入首屏。新偏好消息按消费者懒加载。
- 需要部署时执行新增users列的迁移并重启API；本次未重启共享开发栈，不含新SMTP验证、图片模型调用或外部发布。资料保存语义不恢复团队/工作区/二次解锁；商品输出语言不受界面偏好影响。
- 交付定位随本任务提交。主代理docs-check已通过，最终源码指纹已核对；自建真实集成库/API/Web均已结束，pf_prefs_root_0908及派生测试库已清理。

## 本轮分工

主代理持有跨层合同、共享文档、Git 与最终审核。account_frontend（Astra low）独占 Web Account/preferences/认证接入、API/types/i18n 与相关测试；account_backend（Luna max）独占 auth 账户偏好与商家名称读写、必要 schema 及测试。不改站点 provider、商品输出语言、Agent 或图片业务。独立 PG 前缀 pf_prefs_0908，Web 浏览器端口 29383；最终真实集成由主代理使用独立前缀/端口。接口先核当前读写者，由主代理冻结后实施。执行者不提交、不覆盖他人修改，交付自审证据后等分配。

## 冻结接口与交互（2026-09-08）

- 已认证 session 与 GET `/api/account` 顶层返回 `preferences:{locale,theme}`。新账号及原有未设置账号默认 zh-CN/system；locale 仅 zh-CN/en-US/ja-JP/vi-VN，theme 仅 light/dark/system。未认证会话不承载账号偏好。
- PATCH `/api/account/preferences` 接受非空部分字段 `{locale?,theme?}`，成功返回完整 preferences；拒绝未知字段、null、空对象或非法枚举，未提供字段不改。字段级最后成功写入生效，无 revision。仅当前账号，不能提交目标 ID。
- PATCH `/api/account/merchant` 接受 `{name}`，trim 后 1–160 字符，返回已有 MerchantView。从当前账号直接归属定位并锁定商家；无归属 Operator 返回403，暂停商家名称写入沿现有停用写语义返回403。偏好修改不依赖商家启用。原 PATCH `/api/account` 显示名合同保持。
- 顶栏和账户页选择语言/主题即保存，复用同一 Provider 写入路径，成功后应用服务器值，失败保留已保存值并显示可重试错误。pending 时避免同页面并行偏好写入；不为两字段建立新队列框架。
- 已认证后仅投影服务器偏好，不自动上传匿名 localStorage，也不把账号偏好写回匿名槽。注销恢复匿名浏览器设置或默认，换账号不继承前一个账号的偏好。迟到回调核当前账号与世代；账户其它资料保存只更新其拥有的字段，不能用旧响应覆盖新偏好。
- 商家名称独立草稿/保存；本人显示名、商家名称、偏好和站点参数分别保有真实归属。省略无业务读者的时区与组织设置。

最终个人账户边界修正：商家经营写入停用不应阻断本人安全，账户路由免商家写入门；自有商家名称用例仍在锁内验证停用状态。该用例及商家名拒绝、改密后旧会话失效已共同回归。
