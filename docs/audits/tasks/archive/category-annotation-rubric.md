# 任务：按实际展示目的校准图片评委

状态：完成
类型：实现
认领者：主代理-image-rubric
认领于：2026-09-08T05:49:22+08:00
业务组：图片质量
父账本：image-quality.md
完成后可拆：无

任务遵循 [协议](../README.md)。

## 问题来源与结果

[重复判读](category-annotation-repeatability.md) 的48项暴露：仅2/8候选胜负一致，型号与系列文字被当作身份冲突，类目候选检查点被扩成每张图必须包含的参数，不同detail展示目的混比。本任务在评委提示词的真实决策点修正这些判断，用固定材料重复验证，保留不确定性。

## 前置与并行

输入为上一任务同16图位×3次记录；固定selection/图片/评分schema/四维比较算法/模型gpt-5.6-luna不变，候选只改评委提示词。另在改提示词前固定3次身份负控：美妆商品身份/质量参考配家电候选，标为测试负控，不能混入真实生成质量统计。独立产物storage-dev/image-annotation-rubric-0908；仅调用评委，不占数据库/生图/Agent或账户服务。

## 修改范围与所有权

主代理只改go/internal/imageeval/annotation_judge.go中的评委文字与必要测试；图片质量组/任务证据由主代理维护。不得改选择池/原始图片/生产生成链/Agent Skill/评分阈值或历史结果。并行账号任务不读写本范围。

## 冻结验收

- 类型和当前展示目的决定适用检查点；不同图种的容量/面板/包装字段不自动成为每张图的要求。可观察缺陷与其业务影响分别表达。
- 品牌/型号/系列/营销名区分；没有证据证明互斥时不把不同名称直接写成mismatch/critical。身份未知保留unknown，不能改成自动匹配。
- 候选对照说明展示目的差异；同属detail不等于展示同一结构，差异不直接当成缺失必需项。
- 同一16图位重复3次，完整记录有效率、身份与比较一致性和分维度范围，不以结果好坏补抽样。重点人工抽读家电卖点身份、美妆细节未知、家电细节对照；不得仅因一分差异宣称质量改善。
- 新负控3/3必须判断明显跨品类身份不符，防止通过一味放宽身份检查降低误报。若失败保留候选与证据待修，不改负控凑通过。
- 包测试与真实模型校准满足后自审提交；校准完成不代表全池/图片生成质量/Agent质量通过。候选代码指纹、全部输入与结果保留。

## 阻塞与交接

- 原因：无。
- 解除条件：不适用。
- 跟进者：主代理。
- 交接：独立评委请求，无共享服务改动。

## 证据

- 交付定位：随本任务提交。
- 审核者 / 结论：主代理自审；提示词明确类别适用项、名称层级、展示目的与四维锚点，未变更评分schema/算法/样本/生产生成。固定实验完整执行并保留失败，局部规则修正通过；评委准确性与自动改进门未通过。
- 命令 / 结果：`go test -C go ./internal/imageeval ./cmd/productflow-image-evals -count=1` 两包通过；构建固定CLI后运行原16图位×3，以及独立身份负控×3。主代理复核源与selection SHA256，九份原始报告不变；完整摘要见本地 `storage-dev/image-annotation-rubric-0908/repeatability.json`、`negative-control-summary.json`、`review.md`。
- 原16图位有效47/48，第三轮男装scene为invalid_output，未补跑；身份一致14/16、关键问题状态一致15/16、候选比较一致6/8（原基线分别15/16、15/16、2/8，有效48/48）。负控3/3为mismatch且critical，不混入生成质量统计。
- 抽读：家电参考卖点不再判型号冲突，但match/unknown仍波动；美妆detail保留unknown；家电候选detail三次indeterminate并说明内外部件展示目的不同，前两次仍给对照分数，第三次才拒绝对照。部分不适用类目项仍进入缺点，不能声称全部误判消除。
- 运行ID：reference `annotation-20260907T215010Z-a71f9139548b`、`annotation-20260907T215430Z-a71f9139548b`、`annotation-20260907T215854Z-a71f9139548b`；comparison对应相同时刻后缀`15db27a8eaf3`；negative-control对应后缀`a7d11c5084bf`。
- Issue 结果 / 剩余缺口：完成局部提示词修正和固定校准实验。未证明评委准确率、全池非劣性、生成图片或Agent质量提升。后续需明确图位展示目的、扩充可复核标注，并补充无效响应的可诊断证据；本次报告未保留失败响应原文，无法确定invalid_output具体原因。
