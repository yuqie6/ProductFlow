# 任务：<独立可验收的结果>

状态：开放
类型：实现
认领者：—
认领于：—
完成后可拆：无

任务合同以本文件为准；认领、阻塞、审核与关闭遵循 [Issue 协议](README.md)。按 [所有权前置规则](README.md#认领与并行) 取得已确认的认领后，才开始读取当前实现、追踪调用链、调查根因或设计方案；修改前核对适用仓库规则、测试和 diff。

## 问题来源

<用户提出的现象或需求、已知复现步骤、预期结果；根因未确认时明确写未确认，不猜原因。已有截图、错误信息或关联 issue 可在此引用，不编造会话 ID。>

<来自业务组时，在文件头同时增加 `业务组：<组名>` 和 `父账本：<docs/audits/ 下文件名>`，此处注明来源条款。独立任务省略这两行，看板业务组列填 `—`。>

## 做成什么样

<本次交付的可观察结果，以及独立完成条件。证据类区分采证完成与产品门槛通过。>

## 前置与并行

- 前置：<具体 issue / 条款及所需结果；无则写无>
- 冻结输入：<需要保持不变的代码、任务集、模型配置；无则写无>
- 运行资源：<独占 DB / worker / provider 配置 / 产物路径，或隔离方式；无则写无>

## 只改这些文件

- <已确认的修改路径；根因未知时只授权有界调查，确认根因后补齐实际修改范围。>
- 本文件

## 不要碰

- <本次范围外的合同或模块>

## 现在代码在哪

<路径、调用关系、已有测试与当前缺口。>

## 合同

<用户确认的预期行为与必须保持的现有合同。业务组任务补充父章程相关不变量及条款 ID；独立任务无需章程。>

## 怎么验收

<可执行命令、关键断言、所需环境；明确哪些是 issue 完成条件、哪些只报告结果。>

## 阻塞与交接

- 原因：无
- 解除条件：无
- 跟进者：无
- 交接：无未提交 diff、运行进程或资源占用；实际阻塞时按现场更新。

## 证据

- 命令 / 日期 / 结果：
- 基线 commit / run_id / artifact（适用时）：
- 交付定位：随本任务提交（用 `git log --follow -- <归档任务路径>` 查询）；已有独立候选 commit 时填写其 hash，不为回填本次 hash 另提 commit。
- 审核者 / 结论（自审须注明）：
- Issue 结果 / 业务门槛结果 / 剩余缺口：
