# ProductFlow 用户指南

产品内 `/help` 提供同一套当前说明。本文适合在仓库中检索和排障。

## 1. 第一次使用

1. 使用 `ADMIN_ACCESS_KEY` 登录。
2. 进入 `/settings`，使用 `SETTINGS_ACCESS_TOKEN` 解锁。
3. 创建供应商档案，填写 Base URL、API Key、能力和默认模型。
4. 为 `prompt`、`agent`、`image` 三个用途选择档案、接口和模型。
5. 保存后回到 `/products`。

三个用途都直接参与核心流程：

- Prompt：提示词节点。
- Agent：商品需求澄清、图库整理和工作流创建。
- Image：工作流生图和连续生图。

## 2. 创建商品

进入 `/products/new`。

### 2.1 选择图片计划

- 填写商品名称。
- 勾选需要的图片类型。
- 每种类型默认数量为 2，可用加减按钮或输入框独立修改。
- 多张主视觉通常是候选；多角度图片应在后续对话中说明每张的视角或内容目标。

### 2.2 上传真实参考图

- 上传 1 至 6 张实际商品图片。
- 支持 PNG、JPEG 和 WebP。
- 图片应能看清商品外形、结构、颜色和关键细节。
- Logo、认证、包装、工厂或说明书等内容只有在用户实际提供时才能作为依据。

参考图是商品事实来源，也是后续参考节点的候选资产。商品封面会自动选择，不需要单独上传“主图道具”。

### 2.3 与 Agent 澄清

Agent 会结合图片类型和参考图检查信息缺口。常见问题包括：

- 价格、品类、规格和核心卖点。
- 视觉风格、色彩、字体、摄影和真实感要求。
- 图片内文字语种、文案密度和禁用内容。
- 比例、画质、参考保真度和背景要求。
- 同类型多张图是候选抽取，还是不同角度/内容。

Agent 只应追问会改变工作流或图片结果的信息。用户可以继续补充文字，也可以在商品图库中增加可验证素材。

### 2.4 确认草案

确认面板会汇总：

- 商品事实和冲突。
- 图片类型与数量。
- 统一视觉体系。
- 每类或每张图片的目标和提示词。
- 参考图绑定。
- 比例、分辨率、质量、文字策略和交付规格。
- 计划创建的文件夹、节点和连线。

确认后系统开始物化工作流。画布会按事件逐步显示文件夹、节点和连线，然后进入商品工作台；右侧继续原来的 Agent 对话。

## 3. 商品工作台

### 3.1 四类节点

- 商品上下文：提供确认过的商品事实和视觉体系。
- 参考图：绑定商品图片库中的一张明确图片。
- 提示词生成：把商品事实、视觉规则和图片目标整理成可编辑提示词。
- 图片生成：保存比例、分辨率、质量、背景、文字策略、参考保真度和执行状态。

### 3.2 画布操作

- 从工具栏添加节点。
- 从输出连接点拖到目标输入点创建连线。
- 拖动节点改变位置；滚轮或触控缩放，空白处平移。
- 使用 Ctrl/Cmd/Shift 点选或框选多个节点。
- 删除选中节点或连线前检查其下游依赖。
- 使用自动布局整理排线。
- 用画布文件夹收纳一个局部流程。文件夹只整理视觉布局，不改变 DAG 执行顺序。

节点卡片只显示适合扫描的摘要。完整内容在右侧详情面板编辑，避免卡片因大量表单失去可读性。

### 3.3 节点详情

参考图节点：

- 从商品图片库选择一个资产。
- 查看当前绑定图。
- 需要新参考时先上传或把连续生图结果保存到商品，再绑定。

提示词节点：

- 查看当前提示词产物。
- 编辑图片目标、构图、内容、文字和氛围要求。
- 重新运行后保留版本记录。

生图节点：

- 设置比例和分辨率档位。
- 设置质量意图、参考保真度、背景和文字策略。
- 当文字策略需要图片文字时，填写明确语种。
- 运行后在节点和图库中查看结果。

### 3.4 运行

- “运行工作流”按 DAG 依赖执行全部目标节点。
- “运行当前节点”只提交选中节点所需的执行范围。
- 运行面板显示状态、节点结果和安全错误信息。
- 运行中可取消；可重试的失败提供重试操作。

## 4. 视觉体系与提示词

视觉体系是整个工作流的共享约束，适合记录：

- 风格定位。
- 主色、辅助色、点缀色和背景。
- 字体和字号层级。
- 装饰语言、图标和留白。
- 光线、景深、镜头和构图。
- 分辨率、商业质量、真实感和商品形态锁定。

同类图片可以共用目标和基本构图。需要不同角度、不同内容或不同文字的图片应有单独提示词。单图例外必须明确记录，避免悄悄破坏统一视觉。

## 5. 商品图片库

工作台的图库面板采用资源管理器式结构。

### 5.1 浏览和分类

- 系统目录：全部图片、最近生成、上传图、生成图等。
- 图片类型目录：按创建时选择的业务类型分类。
- 来源目录：按 upload、workflow generation、image session attach 分类。
- 用户文件夹：可创建、重命名、删除和移动图片。

可以切换网格/列表视图，按名称或时间排序，搜索当前范围并分页加载。

### 5.2 图片操作

- 单图预览、重命名和下载。
- 多选当前已加载图片并移动或下载 ZIP。
- 从图片生成交付规格文件。
- 把明确资产绑定到某个参考图节点。

所有生成结果都保留在图库中。系统不定义“废稿”状态，也不会自动删除未选中的候选。

### 5.3 Agent 整理图库

Agent 可以读取有界的文件名、目录和元数据，创建/重命名文件夹，重命名或移动资产。Agent 需要判断视觉内容时会选择具体图片读取，不会一次加载整个图库。

## 6. 保存工作流配方

工作台可以保存：

- 完整工作流配方。
- 一个文件夹对应的局部流程。
- 当前多选节点形成的局部片段。

配方保存结构、连线和可复用配置，不保存商品身份、商品图片或生成结果。配方库中只出现用户主动保存的内容。

## 7. 连续生图

进入 `/image-chat`。

1. 新建或选择会话。
2. 填写提示词。
3. 选择尺寸和候选数量。
4. 可选一个已完成结果作为分支基图。
5. 最多选择 6 张会话上下文图；有分支基图时它会占一个上下文名额。
6. 按当前 provider 能力设置高级字段。
7. 提交后查看排队、运行、候选和供应商说明。

已完成候选可以：

- 设为下一轮分支基图。
- 下载。
- 收藏到 Gallery。
- 选择商品并保存到该商品图片库。

保存到商品后，它与上传图和工作流结果使用相同的 ProductImageAsset 管理方式。

## 8. 设置页

### 8.1 供应商档案

档案包含：

- 名称和 provider 类型。
- Base URL 与 API Key。
- 能力列表。
- 默认模型和 provider 配置。
- 启用/停用状态。

API Key 保存后不回显。更新档案时留空不会把已保存 secret 返回到浏览器。

### 8.2 用途绑定

每个用途选择档案、接口模式、模型和请求参数。绑定要求所选档案具备对应能力。

### 8.3 运行时配置

设置页还管理：

- 图片工具允许字段和默认值。
- 最大生图单边。
- 上传大小、像素和数量限制。
- 并发和安全开关。
- 当前格式的配置导入导出。

图片生成数量由图片计划或会话候选数设置，不在高级图片字段中重复配置。

## 9. 历史归档

`/history` 只提供已退役 V1 workflow、用户模板和 Canvas Agent 记录的查看、筛选、下载与导出。历史图片只有在 canonical 媒体核验完成后才能预览或下载；缺失和待核验媒体仍保留元数据说明。

“用 Agent 重建”会为原商品或用户选择的目标商品创建一个带历史 seed 的 WorkflowDraft 和 AgentConversation。用户仍需检查 Agent 追问与结构化 Draft 并明确确认；该操作不会恢复旧画布、旧模板应用或旧执行器。

## 10. 常见问题

### Agent 创建页无法开始

检查是否选择至少一种图片类型并上传了 1 至 6 张有效商品图。再检查 Agent 用途绑定和 Agent service 健康状态。

### Agent 有回复但没有工作流

查看 Turn 是否处于 requires_input 或 awaiting_confirmation。回答问题并确认最新 WorkflowDraft revision 后才会物化。

### 参考图节点为空

打开商品图库，确认目标图片存在，再从节点详情绑定具体资产。封面不会自动充当所有参考节点。

### 生图失败

检查 Image 用途绑定、模型、参考图、生成规格和错误摘要。尺寸或高级字段不受当前 provider 支持时，修改节点配置后重试。

### 工作流排线难读

使用自动布局，把一个图片类型或局部阶段放入文件夹，并减少跨文件夹的无意义连线。文件夹不需要改变节点依赖。

### 设置保存失败

确认已经用独立 SETTINGS_ACCESS_TOKEN 解锁，并检查 provider 能力、模型和请求字段校验错误。

### 日志是否保存完整 secret

应用日志不应记录 API Key、session token、完整上传字节或 data URL。发现包含 secret 的日志应按安全问题处理。
