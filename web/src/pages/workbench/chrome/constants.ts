export const MIN_INSPECTOR_WIDTH = 280;
export const MAX_INSPECTOR_WIDTH = 560;

/** Inspector 左侧 icon rail 的固定宽度（desktop），shell 与 inspector 之间共享的单一事实来源。 */
export const INSPECTOR_RAIL_WIDTH = 72;

export const IMAGE_PREVIEW_SURFACE_CLASS_NAME = "image-preview-surface";
